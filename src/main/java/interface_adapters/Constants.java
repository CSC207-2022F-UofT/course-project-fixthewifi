package interface_adapters;

public interface Constants
{
    // This is the separator for the content (i.e. the third argument to comManager.send()) of a message, use it to seperate data (see send_chat adapter as an example.)
    char CONTENT_SPR = 30;
}
